# Cloudflare Pages Configuration
name = "portfolio"
compatibility_date = "2024-01-01"

[build]
command = "npm run build"
cwd = ""
watch_dir = "blogs"

# For Cloudflare Pages, redirects are handled in _redirects file
